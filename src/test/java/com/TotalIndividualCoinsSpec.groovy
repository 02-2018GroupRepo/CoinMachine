package com

import spock.lang.Specification


class TotalIndividualCoinsSpec extends Specification {
    
    def "Find total individual coins by location"(){
        
        given: "A location"
        String location = "Atlanta"
        
        when: "an operator calls checkMoney"

        
        then: "the machines return the amount of each individual coin"
    }
}